# Copy this file to .env and fill in real values.
# .env is gitignored — never commit real secrets.

# --- LLM provider ---
API_KEY=sk-...

# --- Database ---
POSTGRES_USER=biorag
POSTGRES_PASSWORD=biorag_dev_password
POSTGRES_DB=biorag
POSTGRES_HOST=localhost
POSTGRES_PORT=5432

# --- App ---
APP_ENV=development